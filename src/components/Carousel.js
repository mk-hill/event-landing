import React from 'react';
import Slider from 'react-slick';

const Carousel = () => {
  const settings = {
    dots: true,
    speed: 500,
    infinite: true,
    autoplay: true,
  };
  return (
    <div>
      <p>asd</p>
    </div>
  );
};

export default Carousel;
